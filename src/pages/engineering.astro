---
import CategoryLayout from '../layouts/CategoryLayout.astro';
import { CATEGORIES } from '../consts';

const categoryKey = 'engineering';
const categoryConfig = CATEGORIES[categoryKey];
---

<CategoryLayout 
	collection={categoryKey}
	{...categoryConfig}
/> 